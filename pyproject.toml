[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "exscriptor"
version = "0.1.0"
description = "Programmatic infrastructure for agent-assisted digitization of historical scholarly texts: native Gemini Batch VLM transcription, OCR second witness, collation oracle, edition rules."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
dependencies = ["typer>=0.9"]
authors = [{ name = "Julio Alonzom" }]
keywords = ["digitization", "transcription", "ocr", "gemini", "latin", "scholarly-editions", "agents"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Topic :: Text Processing :: Linguistic",
]

[project.scripts]
ex-batch-submit = "exscriptor.gemini_batch_run:app"
ex-batch-poll = "exscriptor.gemini_batch_run:app"
ex-batch-raw = "exscriptor.batch_raw:app"
ex-poll-batch = "exscriptor.poll_batch:app"
ex-witness = "exscriptor.witness:app"
ex-check-markers = "exscriptor.check_markers:app"

[tool.hatch.build.targets.wheel]
packages = ["src/exscriptor"]
